spring:
  profiles:
    active: develop   # 기본 환경 선택

# 개발 환경
---
spring:
  profiles: develop
  # datasource:
    # data: classpath:data-h2.sql   # 시작할때 실행시킬 scripts
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
